import { TestBed } from '@angular/core/testing';
import { EndpointMapper } from './endpoint-mapper';

describe('EndpointMapper', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({});
  });

  it('should be created', () => {
    expect(EndpointMapper).toBeTruthy();
  });
});
