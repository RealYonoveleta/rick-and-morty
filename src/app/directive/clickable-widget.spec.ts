import { ClikcableWidget } from './clickable-widget';

describe('ClikcableWidget', () => {
  it('should create an instance', () => {
    const directive = new ClikcableWidget();
    expect(directive).toBeTruthy();
  });
});
